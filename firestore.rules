rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read-only monitor data; only the scheduled function (admin SDK) writes.
    match /monitor/{doc} {
      allow read: if true;
      allow write: if false;
    }
  }
}
